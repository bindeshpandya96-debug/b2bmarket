/**
 * Shared server-side types (re-exports and DTOs).
 * Business entities come from Prisma; this file holds request/response and service contracts.
 */

export type { ListingStatus, OrderStatus, UserRole } from '@prisma/client';
