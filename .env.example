# Database
DATABASE_URL="postgresql://user:password@localhost:5432/b2bsites?schema=public"

# NextAuth (generate: openssl rand -base64 32)
AUTH_SECRET=your-auth-secret-at-least-32-chars
NEXTAUTH_URL=http://localhost:3000

# Order reservation timeout in minutes (orders go EXPIRED if seller doesn't respond)
ORDER_RESERVATION_TIMEOUT_MINUTES=60

# Optional: if set, POST /api/cron/expire-orders must send header CRON_SECRET with this value
# CRON_SECRET=your-secret-here

# Invitation emails (Invite team → Send invitation). Required for sending invite emails.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# SMTP_FROM=noreply@yourdomain.com
# SMTP_SECURE=false
